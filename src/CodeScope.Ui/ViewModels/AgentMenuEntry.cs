namespace NoScope.CodeScope.Ui.ViewModels;

/// <summary>Lightweight row for the agent dropdown: display header + command parameter.</summary>
public sealed record AgentMenuEntry(string Header, string Id);
